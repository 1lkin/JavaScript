//  JavaScript dilində dövr operatorları və işləmə prinsipləri. Dövrlərə aid nümunələr göstərin.
/*
| ------------ | ----------------------------------------- |
| `for`        | Təyin olunmuş sayda təkrarlama            |
                        for (let i = 1; i <= 5; i++) {
                            console.log("Sıra: " + i);
                        }
| `while`      | Şərt doğru olduqca təkrar                 |
                        while (i <= 5) {
                          console.log("While sıra: " + i);
                          i++;
                        }
| `do...while` | Əvvəlcə işləyir, sonra şərti yoxlayır     |
                        do {
                          console.log("Do-while sıra: " + i);
                          i++;
                        } while (i <= 5);
| `for...of`   | Array (massiv) və obyektlər üzərində dövr |
                        let adlar = ["Elvin", "Günel", "Rauf"];
                        for (let ad of adlar) {
                          console.log("Ad: " + ad);
                        }
| `for...in`   | Obyektin açarları (key) üzərində dövr     |
                        let telebe = {
                          ad: "Aynur",
                          yas: 21,
                          ixtisas: "Kompüter elmləri"
                        };
                        
                        for (let acar in telebe) {
                          console.log(acar + ": " + telebe[acar]);
                        }
| ------------ | ----------------------------------------- |

break → dövrü vaxtından əvvəl dayandırır.
continue → cari iterasiyanı keçib növbətiyə keçir.

                for (let i = 1; i <= 5; i++) {
                  if (i === 3) continue;
                  console.log(i); // 3-ü keçəcək
                }

*/
